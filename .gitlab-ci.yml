# The Docker image that will be used to build your app
image: .gitlab-ci.yml
pages:
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main